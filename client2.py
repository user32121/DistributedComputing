#TODO

#like client, but instead is a class, has a submitTask function (submits to local queue, separate thread submits to server)
#calls handleResult callback (inputData, outputData) instead of storing in a dict
